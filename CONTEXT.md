# StarPie

Windows 鼠标轮盘效率工具：按住鼠标拖动弹出径向菜单，松开执行选中扇区绑定的动作。

## Language

### 轮盘与外观

**轮盘 (Wheel)**:
按住鼠标按键、拖动超过触发阈值后在按下点弹出的径向菜单。
_Avoid_: 饼图菜单、环形菜单、RadialMenu

**扇区 (Sector)**:
轮盘上的单个动作槽位，按方位角索引：0° 为正右，沿屏幕顺时针增大；扇区数取 4、8 或 12。
_Avoid_: 按键、按钮、格子

**扇区槽位 (Slot)**:
设置界面中每个扇区对应的一行配置（方向标签与动作）；与「导航槽位」共用「槽位」二字但互不相干。
_Avoid_: 格子

**扇区形状 (Sector Shape)**:
扇区几何的切削形态（原始扇形、独立圆形、圆角胶囊、六边蜂窝），独立于轮盘风格。
_Avoid_: 切削形状（单说时）

**中心核 (Core)**:
轮盘圆心处的圆形区域，可显示图标或图案；把指针拖回其中即取消本次交互。
_Avoid_: 中心圆、核心

**高亮 (Highlight)**:
当前选中扇区的强调呈现，含底色、边框与边缘光晕。
_Avoid_: 选中态、选中高亮

**高亮纹理 (Highlight Texture)**:
叠加在高亮扇区上的纹理图片；尚无设置界面。
_Avoid_: 材质

**核背景图 (Core Background Image)**:
中心核的底图；尚无设置界面。
_Avoid_: 核图片

**轮盘风格 (Wheel Style)**:
轮盘渲染形态模板，决定排版、装饰与绘制方式；不决定扇区几何（见扇区形状）。
_Avoid_: 主题风格、皮肤、主题、界面主题

**轮盘配色 (Wheel Palette)**:
轮盘自身（扇区底色与边框、高亮、文字、光弧）的配色方案，含系统配色与自定义预设，属用户配置数据而非窗口 UI 主题。
_Avoid_: 主题（单说时）、界面主题、皮肤

**预设 (Preset)**:
轮盘配色的用户自定义命名方案：色值组由用户保存，可被配色方案选中并重命名或删除。
_Avoid_: 模板、皮肤

### 程序与动作

**程序 (Program)**:
启动程序动作指向的已安装应用条目，来自程序扫描。
_Avoid_: 应用、快捷方式（特指 .lnk 解析时）

**动作 (Action)**:
扇区绑定的可执行项，类型为快捷热键 / 启动程序 / 打开文件夹 / 系统控制。
_Avoid_: 快捷方式（特指 .lnk 解析时）、命令

**系统动作 (System Action)**:
系统控制类动作可选的内置命令清单（如显示桌面、锁屏、音量）。
_Avoid_: 系统预设

**缺省动作 (Default Action)**:
新建配置方案时按扇区数补齐的占位动作。
_Avoid_: 默认预设

**热键 (Hotkey)**:
动作执行使用的键盘快捷键组合，如 Ctrl+C，由设置界面录制与展示。
_Avoid_: 快捷键（泛称时）、按键

**图标 (Icon)**:
动作与程序条目使用的图形资产，分内置矢量键、用户导入的自定义图标与文件图标提取三类。
_Avoid_: 图片（特指位图文件时）

### 配置方案

**配置方案 (Profile)**:
绑定到特定前台进程（或全局）的一组扇区动作集合。
_Avoid_: 皮肤、布局、方案文件

**全局方案 (Global Profile)**:
前台进程没有专属 Profile 时的兜底方案，进程名固定为 `Global`。

### 交互

**轮盘交互 (Wheel Interaction)**:
按住触发键、拖动超过阈值呼出轮盘、拖动选择扇区、松开执行选中动作 的完整交互序列；拖回中心核或越出逃逸距离即取消。
_Avoid_: 轮盘手势、径向交互

**触发键 (Trigger Button)**:
启动一次轮盘交互的鼠标按键；默认为右键。
_Avoid_: 手势键、手势按钮

**层 (Layer)**（规划中）:
同一方案内可切换的多套扇区动作集合。
_Avoid_: 页、分组

**场景隔离 (Scene Isolation)**:
触发键按下时不启动轮盘交互的前置判定：黑名单进程、指定修饰键、全屏前台任一命中即整次放行。
_Avoid_: 禁用、屏蔽

**白名单模式 (Allowlist Mode)**（规划中）:
场景隔离的反向配置：只在列出的进程内启用轮盘交互。
_Avoid_: 反向黑名单

### 输入栈

**输入栈 (Input Stack)**:
输入侧总体：事件捕获、抑制决策、输入注入与钩子自愈四件事的合称。
_Avoid_: 输入层、钩子模块

**抑制 (Suppression)**:
把触发键事件从系统输入流里拦下、不传给下层应用的语义；必须在事件返回前同步决定，且不可撤销。
_Avoid_: 拦截、吞事件

**补发点击 (Replayed Click)**:
被抑制的触发键点击在该次交互未成立（松开时未越阈值）时补给下层应用的那一次点击。
_Avoid_: 补点击、模拟点击

**注入事件 (Injected Event)**:
由程序注入而非物理设备产生的输入事件（补发点击与动作热键都属此类）；系统与钩子层会标记它，但标记区分不出注入者是谁。
_Avoid_: 模拟事件、合成输入

**回放窗口 (Replay Window)**:
补发点击后等待其注入事件回到输入栈的一小段时间；只吸收带模拟标记的事件，真实输入照常参与交互。
_Avoid_: 忽略期、静默期

**释放结果 (Release Result)**:
触发键松开时输入栈给出的四种处置：放行（不属本交互）、补发点击（交互未成立）、执行（选中了动作）、取消（无选中）。
_Avoid_: 返回值

**钩子线程 (Hook Thread)**:
承载输入捕获回调的专用线程；抑制决策在此同步完成，其余工作一律卸载到别处。
_Avoid_: 钩子回调线程、捕获线程

**钩子看门狗 (Hook Watchdog)**:
周期性探测钩子是否仍活着（事件计数与系统光标位移比对）、发现失效就地重注册的自愈机制。
_Avoid_: 健康检查、心跳

**暂停 (Pause)**:
托盘提供的整机停用语义：钩子照常挂着但全部事件放行，不再启动轮盘交互。
_Avoid_: 禁用、关闭

### 设置台与窗口

**设置台 (Settings Console)**:
启动时创建并显示、关窗即销毁的设置界面会话——一次开启到关闭之间持有窗口与其全部视图模型，再次打开时整个重建；是托盘入口指向的主界面。
_Avoid_: 设置控制台（旧称）、主窗口、主界面

**设置台租户 (Console Tenant)**:
设置台一次开启所对应的那组瞬态对象——窗口、导航区与页面视图模型及其会话作用域，由常驻壳层在开窗时创建、关窗即销毁；下次打开整个重建。
_Avoid_: 租户（单说时）

**页面 (Page)**:
设置台左侧导航列出的顶层区块，是导航的目标单位。
_Avoid_: 分区、Tab、标签页

**导航 (Navigation)**:
设置台内在页面之间切换的机制；各页面的状态在切换间常驻不丢失。
_Avoid_: 切换Tab、页签切换

**导航槽位 (Navigation Slot)**:
导航目录中的固定位置（触发 / 外观 / 轮盘与动作 / 高级 / 插件），与扇区槽位无关。
_Avoid_: 槽位（单独使用时不指扇区槽位）

**窗口外框 (Window Chrome)**:
设置台窗口上不属于页面导航的外围部件：标题与底部操作区。
_Avoid_: 壳窗口（旧称）

**对话框 (Dialog)**:
设置台之上的自绘模态窗口（输入、程序选择、图标选择、取色）；系统文件与文件夹选择器、系统消息框不属此列，全屏取色工具是刻意不设归属的唯一例外。
_Avoid_: 弹窗、子窗口

**插件窗口 (Plugin Window)**:
宿主创建并记账、可非模态显示的插件界面窗口；它不是对话框。
_Avoid_: 插件弹窗、子窗口

**锚窗口 (Anchor Window)**:
永不显示的隐藏窗口，长期占住 `Application.MainWindow`，使瞬态窗口不被自动赋值钉住。
_Avoid_: 主窗口（指设置台窗口时）

**重挂 (Remount)**:
页面在配置导入或重新进入后，按当前运行态配置重新同步内容（回填、重绘）的语义。
_Avoid_: 刷新（指语言切换时的即时文本刷新，二者不同）

**运行态配置 (Runtime Config)**:
内存中的当前生效配置对象；设置改动即时写入其中，落盘后持久化为 `config.json`。
_Avoid_: 当前配置、内存配置

**立即生效 (Live-apply)**:
设置改动即时写入运行态配置并生效的语义，区别于"编辑副本、确认后应用"；与何时落盘是两件事。
_Avoid_: 应用设置

**自动保存 (Auto-save)**:
设置改动写穿运行态配置后延迟落盘的语义；界面没有必须点击的保存操作，两条例外是方案增删改等改动立即落盘、关窗与退出时冲刷挂起的延迟落盘。
_Avoid_: 应用设置、确认保存

### 常驻壳层与系统集成

**常驻壳层 (Resident Shell)**:
进程存活期内一直存在的编排层——托盘、输入栈、插件运行时、单实例接收、退出编排，以及设置台的创建与销毁（启动时建立、关窗后重开时重建）。
_Avoid_: 外壳、后台进程、主窗口容器

**宿主侧 (Host Side)**:
主程序集（exe）中承担宿主角色的部分——组合根、常驻壳层、导航运行时与窗口外框；与零 WPF 的宿主内核程序集 `StarPie.Host` 相对。
_Avoid_: Host（单说时）、宿主程序集

**系统集成 (System Integration)**:
常驻壳层面向操作系统的服务：托盘与气泡、开机自启、提权、单实例、退出编排、内存整理、高级设置面。
_Avoid_: 常驻壳层（整称时）

**托盘 (Tray)**:
系统托盘中的应用图标与菜单（含暂停态 tooltip、双击直达、右键菜单、气泡通知）。
_Avoid_: 系统栏、托盘图标（特指图标资产时）

**界面主题 (App Theme)**:
设置台与对话框的 UI 配色体系；全屏取色工具、托盘菜单与插件窗口不在其列，轮盘另有轮盘配色。
_Avoid_: 主题（单说时）、皮肤、配色方案

### 文案

**声明式文案 (Declarative Copy)**:
挂在界面元素上、由绑定或资源在每次呈现时取词的静态文案；语言切换后由框架自动更新，呈现者无需逐项回填。
_Avoid_: 回填文案

**驻留文案 (Resident Copy)**:
存活于视图模型、语言切换时须由持有者显式刷新（通知）的展示文案。
_Avoid_: 动态文案

**即时取词 (Point-of-use Lookup)**:
仅在内容呈现或事件发生那一刻读取当前语言、此后不再跟随切换的文案（如通知、输入框标题、错误消息）。
_Avoid_: 一次性文案

**壳外文案 (Shell-external Copy)**:
由操作系统级界面（托盘菜单、悬浮提示）而非设置台元素树呈现的文案，在每次展示时读取当前语言。
_Avoid_: 托盘文案

### 权限与单实例

**提权 (Elevation)**:
StarPie 进程以管理员完整性级别运行的形态。它是权限级别本身，不改变配置与动作的语义。
_Avoid_: 管理员模式、Run as Admin（界面文案用语）、提权重启、按需提权

**高权限窗口 (Elevated Window)**:
完整性级别高于 StarPie 自身进程的窗口（如任务管理器、注册表编辑器）；非提权实例既无法在其中呼出轮盘，也无法向其中注入动作，提权实例不受此限。
_Avoid_: 管理员窗口、受保护窗口

**提权自启 (Elevated Autostart)**:
开机自启的一种形态：经 Windows 任务计划程序以最高权限启动，提权由服务在触发时完成，不逐次弹 UAC；与普通权限自启构成互斥的两种自启形态。
_Avoid_: 始终管理员启动、永久提权、开机提权

**立即提权 (Immediate Elevation)**:
提权自启那颗任务的按需触发形态（界面入口为「立即以管理员身份重启」）：不必注销或重启即可得到提权实例，触发过程不弹 UAC；它是提权自启的即时形态，不是第三条权限路线。
_Avoid_: 按需提权、临时提权、提权重启（已废止的 runas 形态，与立即提权不是一回事）

**接管与让位 (Takeover / Yield)**:
提权新实例取代非提权既有实例成为唯一实例的过程；只有「新实例提权且既有实例非提权」构成接管，其余相遇属置前退出。
_Avoid_: 抢占、顶替、双开切换

**置前退出 (Bring-to-front Exit)**:
新实例不构成接管时，把既有实例唤到前台、自身退出的相遇结果。
_Avoid_: 抢占退出

**单实例恢复 (Single-instance Restore)**:
把已运行实例唤到前台的跨进程请求；跨完整性级别投递会被系统拦下，须由接收方按窗口放行。
_Avoid_: 二次启动、唤醒

### 插件

**插件 (Plugin)**:
可在运行期装载与卸载、只经 SDK 与宿主交互的扩展单元；可贡献无界面能力与宿主托管的界面。
_Avoid_: 扩展、模块、加载项

**宿主内核 (Host Kernel)**:
`StarPie.Host` 承载的全部不可卸载运行时与服务；它不是插件。
_Avoid_: 核心、主程序

**插件包 (Plugin Package)**:
插件在磁盘上的一份目录形态；同一插件 id 在安装目录与用户目录各有一份即冲突。
_Avoid_: 安装包、压缩包

**清单 (Manifest)**:
插件包自述身份、版本、能力引用与界面入口的声明。
_Avoid_: 配置、元数据

**发现 (Discovery)**:
启动时扫描安装目录与用户目录、收集候选插件包的过程。
_Avoid_: 扫描（泛称时）、加载

**准入 (Admission)**:
插件是否被允许装载的裁决；三种准入模式（内置 / 审核清单 / 开发者模式）之外未通过即拒绝，模式与拒绝合称准入四态。
_Avoid_: 许可、审核（专指审核清单时）

**审核清单 (Review Catalog)**:
宿主侧签名维护、按插件 id 与版本裁决是否放行的清单。
_Avoid_: 白名单（单说时）、信任列表

**开发者模式 (Developer Mode)**:
默认关闭的显式开关，开启后允许装载未命中审核清单的插件。
_Avoid_: 调试模式、开发模式

**装载 (Load)**:
把清单已校验的插件载入并启动的过程；对应停用时的卸载。
_Avoid_: 加载、启用

**停用 (Disable)**:
在安全点卸载插件、保留其包与宿主状态的语义。
_Avoid_: 禁用、关闭、卸载（专指这一动作时）

**彻底移除 (Uninstall)**:
停用之后连包、配置段、插件数据与宿主状态条目一并删除的语义。
_Avoid_: 卸载、删除（单说时）

**隔离 (Quarantine)**:
清理或卸载验证失败后插件被停用、不再调用、等待显式重试或停用的状态；与轮盘的「场景隔离」无关。
_Avoid_: 检疫、熔断（后者是触发原因）

**熔断 (Circuit Break)**:
能力连续调用失败后守卫中止继续调用并转入隔离的动作。
_Avoid_: 隔离、降级

**重试 (Retry)**:
对隔离插件显式发起的回收续做与重新装载；隔离态不会自行恢复。
_Avoid_: 恢复、重载

**重载 (Reload)**:
对活动插件按当前包重新装载的动作。
_Avoid_: 刷新、重启

**更新 (Update)**:
安全点卸载后装载新版本的动作；界面插件的新版本本进程不能生效，只能登记为挂起版本。
_Avoid_: 升级、重载

**挂起版本 (Pending Version)**:
已就位但本进程不能生效、须下次启动才装载的新版本。
_Avoid_: 待更新、暂存版本

**待重启 (Pending Restart)**:
挂起版本已就位时的用户可见状态；与「已停用」不是一回事（此时用户意图仍是启用）。
_Avoid_: 未生效

**生命周期状态 (Lifecycle State)**:
单个插件从发现到卸载的状态轨迹；隔离是终态，不由时间或重启自动离开。
_Avoid_: 运行状态（管理面投影用语）

**安全点 (Safe Point)**:
无在途插件调用、配置已落盘、宿主侧引用可清的卸载时机。
_Avoid_: 空闲时刻

**危险区 (Danger Zone)**:
卸载链上「插件代码不得再运行」的一侧；进入后没有回头路。
_Avoid_: 不可逆点

**泄漏验证 (Leak Verification)**:
卸载后核对界面资产账本清零、全局根无插件残留的判定。
_Avoid_: 泄漏扫描、内存检查

**残留 (Residual)**:
泄漏验证失败时仍活着的可定位对象，是隔离原因的一部分。
_Avoid_: 垃圾、泄漏（单说时）

**能力 (Capability)**:
宿主声明、插件实现的无界面扩展点。
_Avoid_: 接口、服务

**扩展点 (Extension Point)**:
宿主固定下来、允许插件注册的接入位置（导航页、设置区、托盘菜单项、插件窗口、内容容器）。
_Avoid_: 插槽（那是导航与扇区的词）、挂载点

**界面资产 (UI Asset)**:
插件经宿主注册的界面对象；宿主按插件 id 记账，卸载即按账本摘除。
_Avoid_: 资源（专指资源字典并入资源根时）、插件 UI 资产

**资产登记表 (Asset Registry)**:
宿主按插件 id 记录全部已注册界面资产的账本；是卸载清理与泄漏验证的唯一依据。
_Avoid_: 资产清单

**受支持特性白名单 (Certified Feature Set)**:
已认证可可靠卸载的界面特性集合；白名单外的特性不受支持、不进入验收。
_Avoid_: 支持列表、特性列表

**资源根 (Resource Root)**:
宿主为每个插件创建的容器，插件的资源字典全部并入其中。
_Avoid_: 资产

**宿主门面 (Host Facade)**:
插件唯一可触达的宿主能力集合，分无界面与界面两套上下文。
_Avoid_: API、SDK

**宿主服务作用域 (Plugin Service Scope)**:
每插件一个的子容器与句柄账本；插件的订阅、回调与动作句柄全部登记于此。
_Avoid_: 容器

**宿主状态 (Host State)**:
宿主权威的插件记录（启用意图、已装版本、包路径、准入来源与隔离），与插件私有配置分开存放。
_Avoid_: 插件配置

**诊断报告 (Diagnostics Report)**:
单个插件的宿主状态、准入结果、隔离原因与残留清单的汇总。
_Avoid_: 日志、启动报告

**启动报告 (Startup Report)**:
每次启动扫描落盘的逐插件准入结果与计数。
_Avoid_: 诊断报告

**内置 (Built-in)**:
准入依据之一：以宿主内置 id 清单认定，安装位置本身不是信任依据。
_Avoid_: 官方、默认

### e2e

**后台运行 (Background Run)**:
e2e 作为独立进程运行、不阻塞发起方终端，产物（日志/报告/退出码）全部落盘的运行形态。
_Avoid_: 挂起运行
