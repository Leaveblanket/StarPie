# StarPie (WinPieGestures)

Windows 鼠标轮盘笔势与效率工具：按住鼠标拖动弹出径向菜单，松开执行选中扇区绑定的动作。

## Language

**轮盘 (Wheel)**:
按住鼠标按键、拖动超过触发阈值后在按下点弹出的径向菜单。
_Avoid_: 饼图菜单、环形菜单、RadialMenu

**扇区 (Sector)**:
轮盘上的单个动作槽位，按方位角索引。
_Avoid_: 按键、按钮、格子

**手势 (Gesture)**:
按下 → 拖动超过阈值 → 拖动选择 → 松开执行 的完整交互序列。
_Avoid_: 滑动、拖拽

**配置方案 (Profile)**:
绑定到特定前台进程（或全局）的一组扇区动作集合。
_Avoid_: 皮肤、布局、方案文件

**全局方案 (Global Profile)**:
前台进程没有专属 Profile 时的兜底方案，进程名固定为 `Global`。

**动作 (Action)**:
扇区绑定的可执行项，类型为 启动程序 / 热键 / 系统命令。
_Avoid_: 快捷方式（特指 .lnk 解析时）、命令

**立即生效 (Live-apply)**:
设置改动即时写入运行态配置并生效的语义，区别于"编辑副本、确认后应用"。

**自动保存 (Auto-save)**:
设置改动写穿运行态配置后，经短暂防抖自动落盘的语义；设置界面无显式"保存"操作。
_Avoid_: 应用设置、确认保存

**运行态配置 (Runtime Config)**:
内存中的当前生效配置对象；设置改动即时写入其中，落盘后持久化为 `config.json`。
_Avoid_: 当前配置、内存配置

**页面 (Page)**:
设置控制台左侧导航列出的顶层区块，共五个：触发与场景、外观与形态、手势与动作、高级与系统、关于与更新。页面是导航的目标单位，每个页面有独立的 ViewModel 与 View。
_Avoid_: 分区、Tab、标签页

**导航 (Navigation)**:
设置控制台内在页面之间切换的机制；各页面的状态在切换间常驻不丢失。
_Avoid_: 切换Tab、页签切换

**壳层 (Shell)**:
设置控制台窗口中页面之外的窗口职责集合：窗口生命周期（关窗驻留托盘、淡入淡出）、界面主题应用、导航外围与壳层文本本地化。
_Avoid_: 外壳、主窗口容器

**界面主题 (App Theme)**:
设置控制台与对话框等窗口的 UI 配色体系：由同 key 令牌集（Light / Dark / MidnightNavy / RoyalViolet / TitaniumGray）加“跟随系统 (System)”模式构成。
_Avoid_: 主题、皮肤、配色方案

**轮盘配色 (Wheel Palette)**:
手势轮盘自身（扇区底色与边框、高亮、文字、光弧）的配色方案，含系统预设与自定义预设，属用户配置数据而非窗口 UI 主题。
_Avoid_: 主题、界面主题、皮肤

**主题风格 (Wheel Style / UiStyle)**:
轮盘渲染形态模板（ClassicRing / CleanSectors / Glassmorphism 等），决定扇区切削形状、排版与绘制方式。
_Avoid_: 皮肤、主题、界面主题

**重挂 (Reload/remount)**:
页面在配置导入或重新进入后，按当前运行态配置重新同步内容（回填、重绘）的语义。
_Avoid_: 刷新（指语言切换时的即时文本刷新，二者不同）

**声明式文案 (Declarative Copy)**:
界面静态展示、语言切换后自动随当前语言更新的文案，呈现者无需在切换时逐项回填。
_Avoid_: 回填文案

**驻留文案 (Resident Copy)**:
长期存活于视图模型、语言切换时必须同步更新的展示文案（如导航标题、动作类型选项）。
_Avoid_: 动态文案

**即时取词 (Point-of-use Lookup)**:
仅在内容呈现或事件发生那一刻读取当前语言、此后不再跟随切换的文案（如通知、输入框标题、错误消息）。
_Avoid_: 一次性文案

**壳外文案 (Shell-external Copy)**:
由操作系统级界面（托盘菜单、悬浮提示）而非设置控制台界面元素树呈现的文案，在每次展示时读取当前语言。
_Avoid_: 托盘文案

## Hard Constraints

- `config.json` 格式向后兼容：已发布版本存在存量用户配置。
