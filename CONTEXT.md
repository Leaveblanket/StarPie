# StarPie

Windows 鼠标轮盘笔势与效率工具：按住鼠标拖动弹出径向菜单，松开执行选中扇区绑定的动作；另有一条并列的手势线，按下触发键拖出轨迹、按方向分段匹配动作。

## Language

**轮盘 (Wheel)**:
按住鼠标按键、拖动超过触发阈值后在按下点弹出的径向菜单。
_Avoid_: 饼图菜单、环形菜单、RadialMenu

**扇区 (Sector)**:
轮盘上的单个动作槽位，按方位角索引。
_Avoid_: 按键、按钮、格子

**轮盘交互 (Wheel Interaction)**:
按住触发键、拖动超过阈值呼出轮盘、拖动选择扇区、松开执行选中动作 的完整交互序列。
_Avoid_: 轮盘手势、径向交互

**手势 (Gesture)**:
按下触发键后拖出轨迹、按方向分段并匹配到动作的交互序列；与轮盘交互并列，二者各由自己的触发键启动。
_Avoid_: 轨迹手势、滑动、拖拽

**轨迹 (Trail)**:
手势进行中指针划过的路径；实时绘制在浮层上，让用户看见自己画了什么。
_Avoid_: 笔画、线条

**图样 (Pattern)**:
轨迹按方向分段后得到的序列，是手势匹配动作的键。
_Avoid_: 图案、手势码

**配置方案 (Profile)**:
绑定到特定前台进程（或全局）的一组扇区动作集合。
_Avoid_: 皮肤、布局、方案文件

**全局方案 (Global Profile)**:
前台进程没有专属 Profile 时的兜底方案，进程名固定为 `Global`。

**动作 (Action)**:
扇区绑定的可执行项，类型为 启动程序 / 热键 / 系统命令。
_Avoid_: 快捷方式（特指 .lnk 解析时）、命令

**立即生效 (Live-apply)**:
设置改动即时写入运行态配置并生效的语义，区别于"编辑副本、确认后应用"。

**自动保存 (Auto-save)**:
设置改动写穿运行态配置后，经短暂防抖自动落盘的语义；设置界面无显式"保存"操作。
_Avoid_: 应用设置、确认保存

**运行态配置 (Runtime Config)**:
内存中的当前生效配置对象；设置改动即时写入其中，落盘后持久化为 `config.json`。
_Avoid_: 当前配置、内存配置

**页面 (Page)**:
设置控制台左侧导航列出的顶层区块，是导航的目标单位。
_Avoid_: 分区、Tab、标签页

**导航 (Navigation)**:
设置控制台内在页面之间切换的机制；各页面的状态在切换间常驻不丢失。
_Avoid_: 切换Tab、页签切换

**设置控制台 (Preferences Console)**:
承载页面导航与壳层的设置主窗口（主窗口标题用语：设置控制台 / Preferences Console），是托盘入口指向的主界面。
_Avoid_: 主窗口容器、设置对话框

**壳层 (Shell)**:
应用外壳职责的总称，含两个子域：壳窗口与系统集成。
_Avoid_: 外壳、主窗口容器

**壳窗口 (Shell Window)**:
壳层的窗口职责子域：设置控制台主窗口的窗口生命周期（关窗即销毁、托盘驻留由常驻壳层承担）、界面主题应用、导航外围与壳层文本本地化。
_Avoid_: 外壳、主窗口容器

**系统集成 (System Integration)**:
壳层的系统级服务子域：托盘与气泡、开机自启、内存整理、高级设置面。
_Avoid_: 壳层（仅指窗口职责时）

**提权 (Elevation)**:
StarPie 进程以管理员完整性级别运行的形态。它是权限级别本身，不改变配置与动作的语义。
_Avoid_: 管理员模式、Run as Admin（界面文案用语）、提权重启、按需提权

**高权限窗口 (Elevated Window)**:
完整性级别高于 StarPie 自身进程的窗口（如任务管理器、注册表编辑器）；轮盘交互与手势在其中的可用性取决于 StarPie 自身的完整性级别。
_Avoid_: 管理员窗口、受保护窗口

**提权自启 (Elevated Autostart)**:
开机自启的一种形态：经 Windows 任务计划程序以最高权限启动，提权由服务在触发时完成，不逐次弹 UAC；与普通权限自启构成互斥的两种自启形态。
_Avoid_: 始终管理员启动、永久提权、开机提权

**立即提权 (Immediate Elevation)**:
提权自启那颗任务的按需触发形态（界面入口为「立即以管理员身份重启」）：不必注销或重启即可得到提权实例，触发过程不弹 UAC；它是提权自启的即时形态，不是第三条权限路线。
_Avoid_: 按需提权、临时提权、提权重启（指 `runas` 重启那一种）

**接管与让位 (Takeover / Yield)**:
提权新实例取代非提权既有实例成为唯一实例的过程；只有「新实例提权且既有实例非提权」构成接管，其余相遇属置前退出。
_Avoid_: 抢占、顶替、双开切换

**界面主题 (App Theme)**:
设置控制台与对话框等窗口的 UI 配色体系，包含若干套固定主题与"跟随系统 (System)"模式。
_Avoid_: 主题、皮肤、配色方案

**轮盘配色 (Wheel Palette)**:
轮盘自身（扇区底色与边框、高亮、文字、光弧）的配色方案，含系统预设与自定义预设，属用户配置数据而非窗口 UI 主题。
_Avoid_: 主题、界面主题、皮肤

**主题风格 (Wheel Style)**:
轮盘渲染形态模板，决定扇区切削形状、排版与绘制方式。
_Avoid_: 皮肤、主题、界面主题

**托盘 (Tray)**:
系统托盘中的应用图标与菜单（含暂停态 tooltip、双击直达、右键菜单、气泡通知）。
_Avoid_: 系统栏、托盘图标（特指图标资产时）

**程序 (Program)**:
启动程序动作指向的已安装应用条目，来自程序扫描。
_Avoid_: 应用、快捷方式（特指 .lnk 解析时）

**图标 (Icon)**:
动作与程序条目使用的图形资产，分内置矢量键、用户导入的自定义图标与文件图标提取三类。
_Avoid_: 图片（特指位图文件时）

**对话框 (Dialog)**:
设置控制台之上的一次性模态窗口，是本应用唯一的对话框形态。
_Avoid_: 弹窗、子窗口

**预设 (Preset)**:
轮盘配色的用户自定义命名方案：色值组由用户保存，可被配色方案选中并重命名或删除。
_Avoid_: 模板、皮肤

**热键 (Hotkey)**:
动作执行使用的键盘快捷键组合，如 Ctrl+C，由设置界面录制与展示。
_Avoid_: 快捷键（泛称时）、按键

**重挂 (Reload/remount)**:
页面在配置导入或重新进入后，按当前运行态配置重新同步内容（回填、重绘）的语义。
_Avoid_: 刷新（指语言切换时的即时文本刷新，二者不同）

**声明式文案 (Declarative Copy)**:
界面静态展示、语言切换后自动随当前语言更新的文案，呈现者无需在切换时逐项回填。
_Avoid_: 回填文案

**驻留文案 (Resident Copy)**:
长期存活于视图模型、语言切换时必须同步更新的展示文案（如导航标题、动作类型选项）。
_Avoid_: 动态文案

**即时取词 (Point-of-use Lookup)**:
仅在内容呈现或事件发生那一刻读取当前语言、此后不再跟随切换的文案（如通知、输入框标题、错误消息）。
_Avoid_: 一次性文案

**壳外文案 (Shell-external Copy)**:
由操作系统级界面（托盘菜单、悬浮提示）而非设置控制台界面元素树呈现的文案，在每次展示时读取当前语言。
_Avoid_: 托盘文案

**后台运行 (Background Run)**:
e2e 作为独立进程运行、不阻塞发起方终端，产物（日志/报告/退出码）全部落盘的运行形态。
_Avoid_: 挂起运行

**输入栈 (Input Stack)**:
输入侧总体：事件捕获、抑制决策、输入注入与钩子自愈四件事的合称。
_Avoid_: 输入层、钩子模块

**触发键 (Trigger Button)**:
启动一次轮盘交互或手势的鼠标按键；轮盘交互与手势各有自己的触发键，默认为右键。
_Avoid_: 手势键、手势按钮

**抑制 (Suppression)**:
把触发键事件从系统输入流里拦下、不传给下层应用的语义；必须在事件返回前同步决定，且不可撤销。
_Avoid_: 拦截、吞事件

**注入事件 (Injected Event)**:
由程序注入而非物理设备产生的输入事件（补发点击与动作热键都属此类）；系统与钩子层会标记它，但标记区分不出注入者是谁。
_Avoid_: 模拟事件、合成输入

**回放窗口 (Replay Window)**:
补发点击后等待其注入事件回到输入栈的一小段时间；窗口内到达的输入事件不参与轮盘交互与手势。
_Avoid_: 忽略期、静默期

**钩子线程 (Hook Thread)**:
承载输入捕获回调的专用线程；抑制决策在此同步完成，其余工作一律卸载到别处。
_Avoid_: 钩子回调线程、捕获线程

**钩子看门狗 (Hook Watchdog)**:
周期性探测钩子是否仍活着（事件计数与系统光标位移比对）、发现失效就地重注册的自愈机制。
_Avoid_: 健康检查、心跳
