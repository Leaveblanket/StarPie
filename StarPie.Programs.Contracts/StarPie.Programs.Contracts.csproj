<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- ADR-0023/#96：M3 程序扫描/SPI 契约程序集（IProgramScanner/ProgramEntry/ProgramCatalog
         + SPI IShortcutTargetResolver）。
         P1.4/#113：WPF 契约件已随 SDK 收口迁入 StarPie.Sdk.Wpf/Services/{Programs,Icons}/
         （命名空间 StarPie.Services.Programs / StarPie.Services.Icons 不变），本工程暂留为
         空壳（无源码、无 ProjectReference），待 M3 归并票（P1.9/#118）撤销。
         TFM / 可空性 / 隐式 using 统一在仓库根 Directory.Build.props。 -->
    <AssemblyName>StarPie.Programs.Contracts</AssemblyName>
  </PropertyGroup>

</Project>
