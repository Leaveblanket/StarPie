name: Build and Test

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    name: Build & Verify StarPie
    runs-on: windows-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v7

    - name: Setup .NET SDK 10.0
      uses: actions/setup-dotnet@v6
      with:
        dotnet-version: '10.0.x'

    - name: Restore Dependencies
      run: dotnet restore StarPie.slnx

    - name: Build Solution (Release)
      run: dotnet build StarPie.slnx -c Release --no-restore

    - name: Run Unit Tests (xUnit)
      run: dotnet test --project StarPie.Tests/StarPie.Tests.csproj -c Release --no-restore

    - name: Publish Standalone Binary
      run: dotnet publish StarPie.Ui/StarPie.Ui.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true -o bin/Standalone

    - name: Upload Build Artifacts
      uses: actions/upload-artifact@v7
      with:
        name: StarPie-Standalone-win-x64
        path: bin/Standalone/StarPie.exe
