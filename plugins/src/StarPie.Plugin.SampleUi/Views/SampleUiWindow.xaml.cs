using System.Windows;

namespace StarPie.Plugin.SampleUi
{
    /// <summary>插件窗口（代码后置仅类声明；动画附着目标 AnimationTarget 由 x:Name 生成）。</summary>
    public partial class SampleUiWindow : Window
    {
        public SampleUiWindow()
            => InitializeComponent();
    }
}
