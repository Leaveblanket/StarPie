<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- ADR-0023/#96：S6 对话框契约程序集（IDialogService + 6 结果 record，纯 C# 契约）。
         P1.3/#112：纯托管契约件已随 SDK 收口迁入 StarPie.Sdk（Services/Dialogs/IDialogService.cs，
         命名空间 StarPie.Services.Dialogs 不变），本工程暂留为空壳（无源码、无 ProjectReference），
         待 S6 对话框架并票（P1.10）撤销。
         TFM / 可空性 / 隐式 using 统一在仓库根 Directory.Build.props。 -->
    <AssemblyName>StarPie.Dialogs.Contracts</AssemblyName>
  </PropertyGroup>

</Project>
